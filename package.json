{
  "name": "attrilive",
  "version": "1.0.0",
  "description": "Authentication service for attri.live subdomain access",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "dev": "nodemon --no-stdin --no-colors api/index.js",
    "dev-logs": "NODE_OPTIONS='--no-buffered-stdout' node api/index.js",
    "start-dev": "./scripts/start-dev.sh",
    "generate-token": "node scripts/generate-token.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "authentication",
    "sso",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "jose": "^4.14.4",
    "node-fetch": "^2.6.12",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
